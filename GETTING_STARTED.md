# 晶礦飾品電商平台 - 快速開始指南

## 📋 系統需求

- Node.js 18+ 
- npm 8+
- MySQL 8.0+
- Git

## 🚀 快速安裝

### 1. 克隆專案
```bash
git clone <your-repo-url>
cd shop
```

### 2. 自動安裝 (推薦)
```bash
./setup.sh
```

### 3. 手動安裝

#### 前端安裝
```bash
cd frontend
cp .env.example .env
npm install
```

#### 後端安裝  
```bash
cd ../backend
cp .env.example .env
npm install
```

#### 後台管理安裝
```bash
cd ../admin
npm install
```

## ⚙️ 環境配置

### 1. 資料庫設置
在 `backend/.env` 中配置資料庫連接：
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=crystal_jewelry
DB_USERNAME=root
DB_PASSWORD=your_password
```

### 2. 創建資料庫
```sql
CREATE DATABASE crystal_jewelry CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. 前端配置
在 `frontend/.env` 中配置 API 端點：
```env
VITE_API_BASE_URL=http://localhost:5000/api
```

## 🏃‍♂️ 啟動服務

### 開發模式 (推薦使用三個終端)

#### 終端 1 - 後端服務
```bash
cd backend
npm run dev
```
🌐 http://localhost:5000

#### 終端 2 - 前端服務  
```bash
cd frontend
npm run dev
```
🌐 http://localhost:3000

#### 終端 3 - 後台管理
```bash
cd admin  
npm run dev
```
🌐 http://localhost:3001

## 📁 專案結構詳解

```
shop/
├── frontend/          # 前台用戶界面 (Vue 3)
│   ├── src/
│   │   ├── components/ # 可重用組件
│   │   ├── pages/     # 頁面組件  
│   │   ├── store/     # Pinia 狀態管理
│   │   ├── router/    # 路由配置
│   │   └── api/       # API 接口
│   └── package.json
│
├── backend/           # 後端 API (Node.js + Express)
│   ├── src/
│   │   ├── controllers/ # 業務邏輯控制器
│   │   ├── models/     # 資料模型 (Sequelize)
│   │   ├── routes/     # API 路由
│   │   ├── middleware/ # 中間件
│   │   └── config/     # 配置文件
│   └── package.json
│
├── admin/             # 後台管理系統 (Vue 3 + Element Plus)
│   ├── src/
│   │   ├── views/     # 管理頁面
│   │   ├── components/ # 管理組件
│   │   └── router/    # 路由配置
│   └── package.json
│
├── database/          # 資料庫相關
│   ├── migrations/    # 資料庫遷移
│   └── seeders/       # 種子資料
│
└── docs/             # 文檔
```

## 🎯 核心功能

### 前台功能
- ✅ 商品展示與分類
- ✅ 購物車管理  
- ✅ 用戶註冊/登入
- ⏳ 訂單管理
- ⏳ 搜尋與篩選
- ⏳ 響應式設計

### 後台功能  
- ⏳ 商品管理
- ⏳ 訂單管理
- ⏳ 用戶管理
- ⏳ 庫存管理
- ⏳ 銷售統計

### API 功能
- ✅ RESTful API 設計
- ✅ JWT 身份驗證
- ✅ 資料庫 ORM (Sequelize)
- ⏳ 文件上傳
- ⏳ 郵件通知

## 🛠️ 開發工具

### 前端技術棧
- Vue 3 + Composition API
- Vite (構建工具)
- Tailwind CSS (樣式)
- Pinia (狀態管理)
- Vue Router (路由)

### 後端技術棧  
- Node.js + Express
- MySQL + Sequelize ORM
- JWT (身份驗證)
- Multer (文件上傳)
- Nodemailer (郵件)

### 開發規範
- ESLint + Prettier (代碼格式化)
- Git 版本控制
- RESTful API 設計原則
- Vue 3 最佳實踐

## 📚 API 文檔

### 認證相關
```
POST /api/auth/register   # 用戶註冊
POST /api/auth/login      # 用戶登入  
GET  /api/auth/me         # 獲取用戶資訊
PUT  /api/auth/profile    # 更新用戶資料
```

### 商品相關
```
GET    /api/products      # 獲取商品列表
GET    /api/products/:id  # 獲取單一商品
GET    /api/categories    # 獲取商品分類
POST   /api/products      # 新增商品 (管理員)
```

### 購物車相關
```
GET    /api/cart         # 獲取購物車
POST   /api/cart/items   # 添加商品到購物車
PUT    /api/cart/items/:id # 更新購物車商品
DELETE /api/cart/items/:id # 移除購物車商品
```

## 🚧 待開發功能

- [ ] 完善後台管理界面
- [ ] 實現支付功能集成  
- [ ] 添加商品評論系統
- [ ] 實現願望清單功能
- [ ] 郵件通知系統
- [ ] 庫存管理系統
- [ ] 訂單狀態追蹤
- [ ] 優惠券系統
- [ ] SEO 優化
- [ ] 性能優化

## 🐛 問題排查

### 常見問題

1. **端口被佔用**
   ```bash
   # 查看端口使用情況
   lsof -i :3000
   lsof -i :5000
   
   # 殺死進程
   kill -9 <PID>
   ```

2. **資料庫連接失敗**
   - 確認 MySQL 服務已啟動
   - 檢查 `.env` 中的資料庫配置
   - 確認資料庫已創建

3. **依賴安裝失敗**
   ```bash
   # 清除 npm 快取
   npm cache clean --force
   
   # 刪除 node_modules 重新安裝
   rm -rf node_modules package-lock.json
   npm install
   ```

## 📞 支援

如果遇到問題，請檢查：
1. Node.js 版本是否正確
2. 資料庫是否正常運行  
3. 環境變數是否正確設置
4. 網路連接是否正常

## 🎉 恭喜！

現在您已經成功設置了晶礦飾品電商平台！
開始您的開發之旅吧！ 💎✨