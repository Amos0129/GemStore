# 開發模式 Dockerfile
FROM node:18-alpine

# 安裝 curl 和 openssl (用於健康檢查和 Prisma)
RUN apk add --no-cache curl openssl openssl-dev

# 設定工作目錄
WORKDIR /app

# 複製 package 文件
COPY package*.json ./

# 安裝所有依賴（包含開發依賴）
RUN npm install

# 複製應用程式碼
COPY . .

# 生成 Prisma Client
RUN npx prisma generate

# 建立 uploads 目錄
RUN mkdir -p uploads

# 暴露端口
EXPOSE 5000

# 啟動開發服務器
CMD ["npm", "run", "dev"]